const db_manager = require("./db-manager.js");
const EventEmitter = require("events");
const emitter = new EventEmitter();
async function getResponse(stream_id, chat) {
  // const system_message =
}
module.exports = { getResponse };
